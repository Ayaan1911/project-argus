version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install fastapi uvicorn boto3 python-dotenv openai scikit-learn pandas numpy requests beautifulsoup4
run:
  command: bash start.sh
  network:
    port: 8080
